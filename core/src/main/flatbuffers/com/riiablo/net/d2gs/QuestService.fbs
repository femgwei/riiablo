namespace com.riiablo.net.packet.d2gs;

enum QuestOperation:uint8 {
  SNAPSHOT,
  NPC_MESSAGE,
  OBJECT_INTERACTION,
  // Authenticated/idempotent interaction with a Warp entity. Appended to
  // preserve existing operation values.
  WARP_INTERACTION,
}

table QuestRequest {
  requestId:ulong;
  operation:QuestOperation;
  targetEntityId:int32 = -1;
  messageIndex:int32 = -1;
}

table QuestResult {
  requestId:ulong;
  success:bool;
  reason:string;
  operation:QuestOperation;
  targetEntityId:int32 = -1;
  messageIndex:int32 = -1;
  questRevision:ulong;
  questRecords:[uint16];
}
